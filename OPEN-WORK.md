# Open work

Standing backlog. One item per line, edited in place - never rewritten wholesale.
Rank in tens. `USER:` outranks `FOUND:`; within an origin, bigger `size` goes first.
Closing an item is `- [x]` plus `| closed: <reason>`; the line stays.

- [x] (2026-09-12) [0] USER: the PCIe structure in the topology view - the bridges, whether a controller is behind one, and the speeds along the path | size: Phase 1 of the approved plan (domain model, fabric assembly, render tree, density switch, TUI, docs) plus Phase 2 (scrub and commit the reporter's B550 capture as `windows-chipset-switch`) | open: the plan at ~/.claude/plans/what-i-miss-is-gentle-graham.md is approved with settled decisions; Phase 0 shipped as ede342e (CI green); Phase 1 commits (a) 2bc04eb and (b) 8b52d14 pushed and CI-green - domain model, fabric assembly, envelope field, render tree with spine + hop columns, density enum/config-key/option/TUI-key, CHANGELOG, README, CONFIG, module_reference, CLAUDE.md all updated | closed: 2026-09-18 in 600ab1d (1.2.14). The view itself shipped in Phase 1 (2bc04eb, 8b52d14) and the shipped skill now describes it: the board line, the three densities with the shipped storage-only default named, both hop symbols with their legends, the device column header, and the capable/running pair read against the port above. Held by tests/test_skill_describes_the_fabric_view.py, which takes every string from the PRODUCER that writes it rather than a typed list. A behavioural RED was unavailable - redcheck reports INHERITED COVERAGE, STRONG, because this repo's own CLAUDE.md teaches the hop symbols - so the prescribed substitute was used, a text check of the artifact. Phase 2 was NOT delivered and is carried on its own line at rank 95, because it is blocked on hardware nobody here holds and that blocker was invisible while it sat inside this item

- [x] (2026-09-17) [5] FOUND: a four-dimension review of the Phase 1 fabric work found 12 defects, none of which any test or CI cell could see | size: 12 defects across the renderer, the CLI wiring, the tree assembly and the docs | closed: 2026-09-17 in 3b4c087..dc57e09, each with a guard seen failing first and most proved by mutation against the code they replace. The renderer: an unread hop claimed `legacy PCI` on every Windows bridge, the address column was exactly as wide as a PCI address so no separator was ever drawn, the deepest drawn row sat two columns right of the rest, the disk header three columns right of its own cells, rows wrapped below about 50 columns, `--expand-virtual` clipped the rows it listed, and `not read` carried no style. The wiring: a global `--tree-density` reached `topology` alone, and the interactive page was laid out for 120 columns whatever the terminal had. The assembly: an Intel VMD domain detached its drives into a phantom root complex, an address-less device produced an empty-labelled root that merged buses, a duplicated address dropped a device, and a kept device under a classless parent vanished. Removed six unconsumed helpers. The guard that claimed one-line-per-device asserted only that no line held a bare severity marker, and the one that claimed no subtree floats was a tautology over any tree the module can build; both are replaced by the invariant they named
- [x] (2026-09-17) [5] USER: push the unpushed commits and watch CI through to a verdict | size: one push plus a CI watch, and whatever a red cell asks for | closed: the user chose "push alone, watch CI to a verdict" on 2026-09-18 and it was done - 19 commits pushed as 7137c04, CI and CodeQL both green, including the three surfaces CI had never seen (a `scripts/` directory, the Textual-driven colour gate, the test that reads the shipped SKILL.md). The spelling commit 2ae8050 was pushed on top and its own run was still going at close; a verdict on it is in the handover, not here. NOTE for the next watch: ci_wait's default --timeout 1500 is too short for this repo, whose macOS cells queued over 40 minutes and made the first watcher report rc=2, could-not-tell, which is not a failure
- [ ] (2026-09-17) [20] FOUND: 1.2.14 is committed and unreleased, and five commits on main are unpushed | size: one push-watch-release cycle; the bump is already done | open: PyPI serves 1.2.12 and v1.2.12 is the last tag, so two versions are now unreleased. The bump to 1.2.14 landed in 600ab1d, which ALSO answers what this line used to ask - the skill rewrite is in it, so `make bump-patch` must NOT run again before the release or it would skip 1.2.14. The unpushed five are docs, the pictures, the backlog and the skill; the local gate is green on the current tree | next: push, watch CI to a verdict by sha AND workflow name (`ci_wait --timeout 3000`; the default 1500 is short for this repo's macOS cells), then `make release`, which tags the CURRENT version - never `make push` on an already-committed version
- [x] (2026-09-17) [10] USER: a box across the bottom of the interactive view showing every value, finding and verdict for the selected line, on Topology, Controllers, Disks, Health, Slots and Trend, and the detail levels cycled least-detailed first | size: seven commits - the density order, render/detail.py, the panel and its wiring, the fabric-lines refactor, a selectable topology, a selectable trend, and the column-derivation fix | closed: 2026-09-17 in 75cc43e..4a00ce1, local and unpushed. Each commit gate-green and its guards proved by mutation. Follow-ups that did not fit are the three lines below
- [x] (2026-09-17) [12] USER: nothing in the interactive view is dimmed, and the palette gets colours with good contrast - headers yellow was suggested | size: one CSS line plus a TUI palette layer and its contrast gate | open: BLOCKED ON THE USER, who has three rendered versions in docs/palette-preview/ (git-excluded) and has not chosen. Measured: the rules dim because the repeated column headers are disabled options and textual dims those; theme.py CANNOT simply be brightened, because test_every_palette_colour_is_legible_on_every_background requires 4.0:1 on four backgrounds including two LIGHT ones and every shipped colour is already at the ~4.2 ceiling for a hue serving black and white. The TUI sets its own background, so only it can carry brighter colours | next: put the three versions to the user again, apply the chosen one as a TUI-only palette, keep the printed palette untouched, and add a gate requiring both palettes to define the same roles | closed: 2026-09-17 in d25b75b. The user chose B (gold header, lifted severity). theme.Palette names the roles once and the printed constants derive from PRINTED, so no printed call site changed; adapters/tui/palette.py holds the interactive instance and swaps a colour by its HEX, which needs no knowledge of the role. The header is the one role that cannot be swapped that way - in print it is the bare string `bold`, which an identifier and a note are too - so it is passed to the tree through Fabric, to the card through render_detail, and to the DataTable header row through the stylesheet, which reads the value from the palette. The gate renders all eight pages and fails on any printed hue in the exported picture, with the interactive six required to appear so a quiet page cannot pass it vacuously; it caught four renderables outside the layer and the two-copies defect behind two of them. Dimming is fixed by naming BOTH option component classes, so equal by construction. The detail panel gained a bottom border in the same commit (user: it ran straight into the key bar)
- [x] (2026-09-17) [15] USER: a drive's size is not written where a reader finds it, and the figure that is there is ambiguous - write it in both bases, like 500GB/466GiB | size: one new formatter, one changed formatter, the card's identity group, and four pinned call sites | open: decided with the user to reach the printed tables too, so the card, the tables and the JSON spell a binary figure one way. format_size is base 1024 and labels GiB as `G`, so a drive sold as 500 GB reads as `466G` beside its own model name | next: theme.format_size gains real units (`466GiB`), new theme.format_size_both returns `500GB/466GiB`, detail.disk_detail moves the size out of the heading into a labelled identity pair; update tests/test_diagnostics.py:836-850, tests/test_tui.py:295, README.md:85-86 and the theme.py doctests | closed: 2026-09-17 in 0ff6e8d, exactly as planned. format_size names its scale (`466GiB`), format_size_both writes `500GB/466GiB` with the decimal figure first because that is what is on the drive's own label, and collapses to one figure below a kilobyte where the scales agree. disk_detail carries it as an identity PAIR and the heading no longer repeats it. Guarded by test_size_in_both_scales and by a panel test asserting the identity group names a size on every drive of every capture, with a control that refuses to pass on a fleet of dashes. The README example block and every picture were regenerated
- [ ] (2026-09-17) [30] FOUND: seven shipped commits have no documentation PROSE pass | size: CONFIG.md mainly, with README, CHANGELOG.md and CLAUDE.md current for the link-figure work and for nothing before it | open: the picture half of this item is DONE and stays done - regenerated in 3da6608 and 0ff6e8d, then again in fec9099 for the one-spelling change, so nothing here is owed a rebuild. All eight were regenerated at the house size of 180x50 in Monospace Regular 9, plus a README animation of one recorded pass through the interface, both cut from one run by scripts/make_demo.py. The SVG-then-magick path recorded here does NOT work for the font - export_screenshot precomputes every glyph run's x from a hardcoded 20px metric - so the pipeline records the pilot run as an asciicast and renders it with agg, which lays the grid out from an explicit family and size | next: the PROSE pass is what is left - CONFIG.md for the panel, the selectable pages, the column derivation, the palette and the units. CLAUDE.md, CHANGELOG.md and the README were brought current for the link-figure work (679b743..d6fb293) and for nothing before it
- [x] (2026-09-17) [40] FOUND: three choices in the palette and picture work the user has not seen or vetoed | size: one CSS line and one style constant, each independently revertible (the rebuild is settled) | open: BLOCKED ON THE USER, raised in chat and not answered. (a) a DataTable's own header row is gold too, which the approved preview could not show because Textual draws that row and it needed CSS, so what they approved has a plain header there; (b) SETTLED 2026-09-18 - the user confirmed 9 POINTS, so the shipped `--font-size 12` stands and nothing is reverted; (c) the detail card's group labels are now bold in the PRINTED view as well, where they used to be the unknown grey | closed: 2026-09-18, all three put to the user one at a time and all three CONFIRMED AS SHIPPED, so no code changed and nothing was reverted. (a) a table's own header row keeps the palette gold, because a header appears in three places and one role keeps one hue. (b) "Monospace Regular 9" is 9 POINTS, so `--font-size 12` stands and the rank-50 rebuild is the spelling alone, every frame keeping its 1315x857 geometry - measured, GitHub's 838px README column downscales a 12px build to 7.6px effective and a 9px build to 7.65px, so the inline copy reads the same either way and only the full-size click-through differs. (c) the printed card's group labels keep their bold, because the unknown grey is reserved to mean "could not be read" and a grey label claimed the group's own NAME was unreadable
- [x] (2026-09-18) [10] USER: on controller, NVMe and SSD, a link states what it is worth - running Gen3x2 (xx GB/s) capable Gen3x4 (yy GB/s), the SSD speeds priced too, the blank dropped from Gen3 x4, and the screenshots redone | size: theme, layout, report, tables, tree, detail, the TUI, the domain, 8 test files and a new one, plus README, CHANGELOG, CLAUDE.md, the shipped skill and nine pictures | closed: 2026-09-18 in 679b743..909095b, local and unpushed. The user chose the widest of three scopes, so the figures carry their throughput in both trees, all three tables and the panel. A link figure is never clipped and its bandwidth is surrendered before any column is dropped, which is what keeps `size` on a 120-column page; the fabric's hop pair has two tiers on the same rule, so below about 100 columns the tree draws what it drew before. The panel's `carries` value is gone - it held the CAPABLE throughput beside the RUNNING link. Closing the figure also exposed that the hop column was sized by its figures and never by its own heading; there is a guard for that now, proved by mutation
- [x] (2026-09-18) [45] FOUND: three views spell a PCIe generation two different ways for the same port | size: one formatter call per view, plus whichever tests pin the old spelling | closed: the user chose the marketing form, so `format_pcie_decimal` is gone and every view - both trees, all three tables, the detail panel and a finding's own sentence - draws through `theme.format_pcie_generation`. Held by tests/test_one_spelling_for_a_generation.py on the RENDERED page, plus a tie between the domain's `_format_pcie` and the render column, because the domain cannot reach the formatter and writes the spelling a second time
- [x] (2026-09-18) [50] FOUND: the eight committed screenshots and the README animation still show the decimal spelling | size: one `scripts/make_demo.py` run at 180x50, plus fetching `agg` | open: the pictures were taken before the spelling was unified, so `1-topology.png` and the animation draw `3.0x4` where the tool now prints `Gen3x4`. `agg` is not on PATH, is not a project dependency and has to be fetched from the asciinema releases for the run, which is an outward download rather than something the repo can do on its own | closed: 2026-09-18 in fec9099. The user approved the fetch, so `agg` 1.9.0 now lives at `~/bin/agg` (already on PATH) and a future rebuild needs no download - `make_demo.py` resolves it with `shutil.which`, and `command -v agg` answers whether it is still there. All nine pictures rebuilt from ONE recorded pass, 17 frames, geometry held at 1315x857 because the user confirmed 9 POINTS (rank 40 (b)). Verified by OCR against the superseded picture as a POSITIVE CONTROL, which is what makes the zero mean something: old 1-topology.png reports 13 decimal figures and 3 Gen forms, the new one 16 Gen and 0 decimal, and 3+13=16 so every decimal figure became the marketing form and nothing else moved. `fc-match` and ImageMagick were already present; no test reads the picture files
- [ ] (2026-09-12) [95] FOUND: the density rules are asserted on four captures, none of them a chipset used as a PCIe switch | size: one Windows capture, scrubbed and committed as the `windows-chipset-switch` fixture, plus the density assertions it would strengthen | open: BLOCKED ON HARDWARE nobody here holds - verified 2026-09-18, no such capture is in tests/fixtures/hw/ or anywhere on this machine. Split out of rank 0 on 2026-09-18 when that item's actionable half closed: it was Phase 2 of the approved plan and read as merely half-done work while it sat inside a live item. Sibling of rank 100, which wants the LINUX capture of the same kind of card | next: on any snapshot of a chipset-as-switch card, scrub it, commit it as that fixture and add the density assertions; until one appears there is nothing to do here
- [ ] (2026-09-18) [90] FOUND: a drive's `achievable` reads as a dash for every NVMe drive | size: one property, or one decision that it should stay | open: `InterfaceLink.achievable_gbps` needs both SATA ends and an NVMe drive has neither, so the panel's link group ends `achievable -` on every NVMe row while the three figures beside it are all read. Whether it should come from the PCIe pair instead is a question about what the value MEANS, not a bug to fix quietly | next: decide whether achievable is about the SATA pairing alone, and if not, give it the PCIe answer
- [ ] (2026-09-12?) [110] FOUND: a CLI-template repo's CI has a flaky macOS / Python 3.14 cell | size: unknown, not this repo's code | open: carried in two handovers and never filed in the repo that owns it, so it lives only here; first seen here in that form | next: reproduce it from that repo's own CI history and file it there, or drop this line with a reason

Host names, key paths and hardware specifics stay OUT of this file - it is tracked in a
public repo. Those live in `handover.md`, which is gitignored.

- [x] (2026-09-10) [10] FOUND: committed hardware captures carried identifying data the scrub had not reached | size: 4 captures plus a ruling on what was already published | closed: 2026-09-10, rescrubbed and pinned by `tests/test_fixture_serials.py`, which decodes every copy and fails when one disagrees. The user ruled to scrub forward and accept the already-published copies rather than rewrite history: a force-push would silently freeze every marketplace install, the same data is in 13 published sdists, and deleting those burns their filenames permanently.
- [x] (2026-09-10) [20] FOUND: 69 documentation findings (60 confirmed, 9 likely) across 11 documents, all evidenced | size: 69 findings, biggest groups are 19 in the contributor set, 13 in CONFIG.md, 8 each in README.md and the shipped skill | closed: 2026-09-10, the 10 repository documents fixed in 8a16ec4 and e84af35, each claim re-measured against the running tool before its wording changed; 2 findings were rejected on that re-measurement rather than applied. The 11th document is the article, now rank 70.
- [x] (2026-09-10) [30] FOUND: the default-page completeness test does not cover the trend section, so deleting it from `render_full` leaves the suite green | size: one test, plus deciding whether `render_full` should be called with history in it | closed: 2026-09-10 in efd90ee - the guard renders the page WITH a history, since without samples the section prints an explanation rather than rows; proved by mutation.
- [x] (2026-09-10) [15] USER: a bug report that lsdsk tells the owner of a PCIe expansion card to replace working hardware, because it reads the card's SATA controller as a 0.25 GB/s uplink | size: one diagnostics rule, plus deciding how the case is detected at all | open: blocked on the reporter, who was asked for the HWiNFO bus view of that controller and a fresh capture with the card refitted. The value is the device's own, not a platform misread: three NVMe siblings on the same switch report real links of differing widths, and the board's own chipset SATA reports a full link. Measured throughput is over four times the figure the tool calls the ceiling. The switch carries a second integrated function, a USB controller, and it publishes the identical floor value, which separates integrated functions from passed-through ports far better than the floor value alone. A published review of that card model measured about 1.7 GB/s across four SATA drives on it, so the reading is refuted independently of the reporter, and the chipset's documented 8 downstream Gen4 lanes account for the NVMe widths, which the integrated functions are not drawn from | next: when the screenshot arrives, decide between a placeholder the device publishes and a misread, then fix the rule; the reply is drafted and waiting to be sent | closed: 2026-09-11 - fixed without waiting on the reporter: when a controller's link reads the PCIe floor in both columns, another function on the same switch reads the identical floor and a third device there has a real link, the oversubscription warning becomes a hint that the figure is a register default. A switch is proven by a bridge recorded above its bus, so root-complex ports are judged as before. The reporter's capture converts, and all five fixtures' findings are byte-identical. Sending the reply was carried as rank 16, which the user removed from the backlog on 2026-09-11
- [x] (2026-09-10) [45] FOUND: the shipped skill teaches documentation as a SUPPLEMENT to what lsdsk cannot measure, never as something that can REFUTE a finding it did make | size: three edits to skills/lsdsk/SKILL.md, plus the GREEN and refactor passes, the checklist artifact and a plugin version bump | open: RED is recorded and failed in three ways - the baseline confirmed a wrong ceiling as measured, looked nothing up although the skill says lane sharing is worth looking up every time, and invented a claim that would send a reader shopping for the wrong hardware. Evidence and the three named edits are in skills/lsdsk/.skillwriter/red-2026-09-10-documentation-refutes-a-finding.md | next: write GREEN against that artifact; the sentence to fix is in "Keep the two apart when you answer", which resolves a conflict with "lsdsk is describing what is actually there" and is the line that sends a reader wrong when the reading is a register on an integrated function | closed: 2026-09-10 in 1.2.7 - four edits written against the RED artifact and verified by re-running its scenario: the run called the figure wrong rather than low, looked the card class up, and reached the opposite conclusion on card-versus-onboard. Refactor closed three gaps that run reported, one opened by the change itself. Evidence in skills/lsdsk/.skillwriter/checklist-2026-09-10-documentation-refutes-a-finding.md
- [x] (2026-09-10) [50] FOUND: nothing at the point of use says a snapshot carries drive serials and the hostname | size: the `snapshot` docstring plus a line in README.md and CONFIG.md | open: only `skills/lsdsk/SKILL.md:465` says it, and `snapshot --help` is where somebody stands before pasting one into a bug report | next: put the decision to the user, then add the sentence where the command is invoked | closed: 2026-09-11 - the user chose the docstring plus a line printed on write. The line goes to stderr in both output modes, so the path line and the JSON envelope on stdout are unchanged; held by three tests in tests/test_snapshot_notice.py, each seen failing first. Correction to this line: the skill's sentence is at SKILL.md:480, not 465, and it names serials but not the hostname - carried as rank 85
- [x] (2026-09-10) [60] FOUND: `[tool.ruff] extend-exclude = ["old"]` excludes a directory the repo no longer has, and its comment describes it as still present | size: one line of pyproject plus its three-line comment | open: found while checking the gate's scope; harmless but it documents a tree that is gone | next: confirm no `old/` exists, drop the key and the comment, re-run the gate | closed: 2026-09-11 - confirmed no old/ exists and git never tracked one; the key and its three-line comment are removed through tomlkit, and the file re-parses with every other key intact
- [x] (2026-09-10) [70] FOUND: 9 findings in the German article draft, which is headed for print | size: 4 confirmed factual errors plus 5 flagged as likely, one of them a claim about third-party software that this repo cannot settle | open: the file is untracked and git-excluded, it is the author's prose rather than repo documentation, and it needs a language pass as well as a factual one | next: put the four confirmed errors to the user and ask whether to edit the draft directly | closed: 2026-09-11 - the audit held 10 items, not 9. Seven are fixed in the draft, each re-verified against the running tool or the source rather than the audit's word, and two are rejected on that evidence (the platform line and the eight-ports measurement both hold). The blocklist claim cannot be settled from this repo; it was carried as rank 75, which the user removed from the backlog on 2026-09-11. The draft is docs/artikel/artikel-ct-lsdsk.md, which is git-excluded, and it shrank from 1370 to 1368 words
- [x] (2026-09-10) [90] FOUND: a TUI test is flaky on one CI cell and reds main at random | size: one test, plus deciding whether the strip rewind or only the assertion is at fault | open: seen once on windows-latest with Python 3.12, `test_moving_the_cursor_winds_the_strip_back_to_the_start` failing `assert 8 == 0` on the WWN strip's scroll offset; a rerun of the SAME commit passed, and the commit before it was green with byte-identical test code, so the cause varies with runner timing rather than with the code. Note the rewind already runs with animate=False and the assertion before it passed, so the handler did run - an animation is NOT the explanation | next: get a second failure before theorising; the pytest step runs continue-on-error, so a red cell shows every step green and only the summary step failing, and the real name is in the job log rather than in `gh run view --log-failed` | closed: 2026-09-11 - root cause fixed in production, not in the test: Textual's scroll_to defers until after the next refresh unless immediate=True, whatever animate says, so the strip painted the new identifier at the old clamped offset before its rewind ran, and a pilot pause could sample in that window. Reproduced through the real pytest node by slowing Textual's update tick, 10 of 80 failed before and 0 of 80 after; a new test watches the offset during the layout that paints the identifier and is red before the fix
- [x] (2026-09-10) [80] FOUND: a `--set` override is displayed with the provenance of the layer it replaced | size: one display path in the config adapter, plus a test | open: found while re-testing the precedence claim; the value is correct, only its reported source is wrong, so `lsdsk config` answers "where did this come from" with the wrong layer | next: give an override its own provenance label and assert it in a test | closed: 2026-09-11 - Config.with_overrides shares the original provenance map by design, so the override path now rebuilds it, naming cli with no path for exactly the keys --set supplied and leaving every other key's source alone. Held by three tests, one driving lsdsk config end to end, each seen failing first
- [x] (2026-09-11) [85] FOUND: the shipped skill says a snapshot contains drive serial numbers but not that it carries the hostname | size: one sentence in skills/lsdsk/SKILL.md through the skill-writer procedure, plus a plugin version bump | open: found while closing rank 50; the tool's help and its write-time notice now name both, so the skill is the one place that states half | next: RED against the current sentence, then add the hostname | closed: 2026-09-11 in 1.2.8 - the content statement names serial numbers and the hostname for both a snapshot and the history store, and the ticket passage warns where a reader is told to send one. Verified by a text check and a quote-back rather than a behavioural run, because this repo's CLAUDE.md already teaches that a capture has a hostname field; evidence in skills/lsdsk/.skillwriter/checklist-2026-09-11-snapshot-carries-the-hostname.md
- [x] (2026-09-10) [95] FOUND: the skill's finding table says an oversubscribed controller's drives "want more than its uplink carries", wording that predates the ceiling becoming what a link can carry rather than what it negotiated | size: one table row in skills/lsdsk/SKILL.md through the skill-writer procedure, plus a plugin version bump | open: first raised in the 2026-09-10 handover and never carried into this file; the new Controller oversubscribed section may already make the row read correctly, which is unverified | next: read the row against the 1.2.6 ceiling change and that section, then RED only if it still reads as the negotiated link | closed: 2026-09-11 - the row no longer offers an HBA as an equal remedy: it says the uplink figure can be a register default, puts moving a drive onto a controller already fitted first, and points to its section as its neighbours do. On reading, "carries" already reads as capacity, which the ceiling now is; the live defect was the row contradicting its own section. Verified by a row-scoped quote-back; evidence in skills/lsdsk/.skillwriter/checklist-2026-09-11-oversubscription-row.md
- [x] (2026-09-11) [55] FOUND: a controller's move and swap advice is given without checking whether the attached drives would notice the gain | size: one decision about _platform_limited_finding in domain/diagnostics.py, plus a rule change and tests if the answer is yes | open: found while correcting the c't article, whose old sentence described load-aware advice the code does not give; _headroom_sentence weighs load only when no better slot exists | next: decide whether a move should depend on load, then change the rule or leave it and say why | closed: 2026-09-11 in f6df447 - decided yes. A move or swap stays a warning only when the attached drives' peak demand reaches what the controller's current seat gives, and is otherwise a hint naming the slot; nothing attached counts as not noticing, an unread link as noticing. A PCIe drive counts at its link capability, because a resting link reads as spare, which also corrected the capped hint's headroom sentence. Across twelve real captures that sentence is the only change, in 6 findings on NVMe drives
- [x] (2026-09-11) [97] FOUND: the oversubscription warning reads "1 drives can pull about ... together" when a controller has a single drive | size: one string in domain/diagnostics.py plus a test | open: pre-existing, found while fixing rank 15, which deliberately left the warning path unchanged | next: take the singular opening the new register-default hint already uses | closed: 2026-09-11 in 2954bb2 - the oversubscription warning and the register-default hint share one clause that speaks of the drive when there is one. The same shape sat in the capped-by-the-mainboard hint's headroom sentence, on both of its branches, and in the trend view's empty state; all four are fixed in the same commit, each with a test seen failing first
- [x] (2026-09-11) [93] FOUND: tests/conftest.py deletes the coverage database in the system temp dir on every pytest run without COVERAGE_FILE, so concurrent pytest sessions on one machine may delete each other's coverage data | size: one fixture plus a check | open: read from the code while fixing rank 90, not measured | next: reproduce with two concurrent runs on the default path before scoping the database per session | closed: 2026-09-11 in 81c85a8 - reproduced: a plain pytest run after a coverage run left No data to report. The hook's COVERAGE_FILE redirect never reached pytest-cov, which creates its Coverage object before any conftest is imported (measured with TMPDIR pointed elsewhere), so the purge was all the hook did; both are removed, and a test runs a real pytest session beside a planted database and failed first. Concurrent pytest --cov runs that leave COVERAGE_FILE unset still share the pyproject data_file, which pytest-cov erases and combines around; the pyproject comment says so, and bmk and CI set their own
- [x] (2026-09-11) [65] FOUND: the register-default hint shipped in 1.2.9 can hide a genuine oversubscription: a truly Gen1 x1 SATA card carrying several drives, behind a real switch beside any unrelated device that also reads the PCIe floor, gets the hint instead of the warning | size: one capture of such hardware, then possibly a tighter twin condition and tests | open: the rule was validated on one real machine, and the user chose to keep it as shipped and validate before tightening (2026-09-11 decision review) | next: capture a real switch carrying a discrete Gen1 x1 controller beside a floor-reading device, replay it, and tighten only if it misfires | closed: 2026-09-11 in 5010418 - checked on real hardware: six machines were captured and none has a PCIe switch carrying a device at the floor, so the misfire could not be reproduced live, but three carry a network controller and two a FireWire controller that each publish exactly 2.5 GT/s x1 in both columns on a working link, which shows the twin leg is met by separate parts. Tightened: the controller and its twin must each carry the vendor of the port in front of it, and those ports must be one maker's; ports now record both vendors on both platforms. The reporter's capture still gets the hint, and no other finding changed across twelve captures
- [x] (2026-09-11) [98] FOUND: the Windows builder's controller_of cannot walk up the device tree it describes | size: one function plus deciding what the reader must record | open: found while typing the capture; the loop returns the start instance when it is a PCI device and otherwise looks it up in the PCI map it was just not found in, so a disk whose parent is not itself a PCI device maps to no controller, and the capture records no parent for a non-PCI device for the walk to follow | next: check a Windows capture with a disk behind a non-PCI parent (a RAID or USB bridge) for an empty controller, then record parents or drop the loop | closed: 2026-09-11 in 8f88f9a - the reader records each disk's ancestry to the root and the builder takes the nearest PCI instance; a USB-bridge builder test failed first. Live on a real Windows machine the new reader recorded the ancestry of all three disks: a file-backed virtual disk walked through its bus driver to the root and maps to no controller, and the NVMe and SATA disks map to the same controllers as before. No USB disk was attached, so the USB mapping itself is proven by the builder test, not live
- [x] (2026-09-11) [99] FOUND: the Linux and Windows builders each define an identical _to_int and _decode_base64 | size: two helpers moved to one shared module, plus their doctests | open: found while typing the builders; the Windows builder already imports parse helpers from the Linux one, so the copies can drift apart silently | next: move both into one hardware decode helper module and import them from both builders | closed: 2026-09-11 in b0192bf - parse_int and decode_base64 live once in adapters/hw/decode/captured.py, with doctests and a parametrised test over the tolerant cases, and both builders import them. parse_pcie_speed and controller_kind_of still live in the Linux builder, which the Windows builder imports them from; that was not this item
- [x] (2026-09-11) [70] FOUND: the capped-by-the-mainboard hint calls a wider port faster for a card that cannot use the width, so a PCIe 4.0 x4 card in a PCIe 3.0 x4 port is told the board has faster ports (PCIe 3.0 x16) and that freeing one would take it to its own maximum | size: _board_best_pcie and the action sentence built on it, plus a test | open: seen on four of twelve real captures while grading moves by load; _board_best_pcie also takes its best speed and its best width from different ports | next: judge each port by what it would give this controller, as _gain_in already does, and assert on a committed capture that no faster port is claimed | closed: 2026-09-11 in 2ff8a37 - each port other than the card's own seat is judged with _gain_in, freeing one quotes what that port gives, and a tie names the more capable port. Where nothing gives more, the detail says so instead of calling the current seat the fastest port, and the upgrade names a board of the card's own generation, or the one port the card needs where the board already has that generation. The same finding had a second defect, naming the next generation up beside the card's maximum, fixed in that commit. Across sixteen real captures four remedies changed, all of them the false free-a-port advice, three details changed wording, and no severity moved
- [x] (2026-09-11) [75] FOUND: an NVMe drive with several namespaces is counted once per namespace, so the oversubscription warning and the capped-by-the-mainboard hint add its demand up several times | size: counting one drive per NVMe controller in the demand sums, plus a test | open: read from the code by a review (Linux lists each namespace as its own block device carrying the controller's link), not reproduced on hardware | next: build a two-namespace inventory, confirm the doubled demand, then count each NVMe controller once | closed: 2026-09-11 in d5a9543 - confirmed, and worse than recorded: summing namespaces weighed a drive's link against itself, so a drive with two namespaces could be called oversubscribed by itself. Inventory.drives_on folds NVMe disks that share a controller and a serial number, the demand sum, the peak check and both drive counts use it, and drives whose serial was not read or that only share a tri-mode HBA still count apart. Adding a second namespace to every NVMe drive in two committed captures changes no controller finding
- [x] (2026-09-11) [10] FOUND: on Linux the PCIe-floor hint could rest on the port in front of the controller, which for a function built into switch silicon reads the floor as its own capability | size: one leg in _floor_twin where the port capability was read, a Linux capture of such a card to validate it, plus tests | open: a hardware report of a chipset used as a PCIe switch card showed both internal ports, in front of its SATA and USB functions, at PCIe 1.1 and 2.5 GT/s x1 capability with no slot implemented and the minimum ASPM exit latencies, while its external ports read PCIe 3.0 and 4.0 capabilities; a physical port carrying a card that really is Gen1 x1 reports its own higher capability, which is the case the vendor check cannot exclude when the card and the switch share a maker. Windows exposes no bridge capability without a kernel driver, so the vendor check stays the evidence there | next: get a Linux snapshot of such a card, confirm sysfs reports the internal port's capability as 2.5 GT/s x1, then decide whether that leg replaces or adds to the vendor check where it was read | closed: 2026-09-12 - it ADDS to the vendor check, and it needed no new hardware: the discriminator is already modelled and already captured on Linux, because a capture records a bridge's own max_link_speed. A port whose capability was READ and reads above the floor now refutes the hint, on the controller's own port and on the twin's; an unread port refutes nothing, so Windows is unchanged. Validated on a committed capture rather than on the reported card: in linux-sas-hba an Intel network controller at the floor sits behind an Intel port reading 5.0 GT/s x1, which today's vendor check wrongly passes. Held by a RED seen failing and an opposite-direction control; 1173 tests pass
- [ ] (2026-09-12) [100] FOUND: the port-capability leg of the PCIe-floor hint is validated in the NEGATIVE direction only | size: one Linux capture of a chipset used as a PCIe switch, then either a committed fixture plus an assertion or a narrowing of one condition | open: shipped in 1.2.12 and kept there on the user's decision of 2026-09-12, to be revisited when such a capture appears. The negative case is measured on linux-sas-hba, where a separate part at the floor sits behind a port reading 5.0 GT/s x1. Nothing here proves the POSITIVE case, that such a card's internal ports publish the floor as their own capability on Linux; that rests on the reporter's hardware report. If they do not, the reported card is told again to replace working hardware, and no test would catch it because that capture was never committed as a fixture | next: on any Linux snapshot of a chipset-as-switch card, read its internal ports' max_link_speed and max_link_width, then commit it as a fixture and assert the hint stands, or narrow the leg
- [x] (2026-09-12) [25] FOUND: a controller that shares its PCIe port with another device was on no port at all, which inverted a severity | size: one join plus two fields, and the two own-seat skips built on the same wrong side | closed: 2026-09-12 in 38abcbd - a port record names ONE of the devices behind it, so asking which port names this controller found nothing whenever a sibling represented the port, and _port_holding, _ports_beside and _floor_twin all returned empty for it. Measured through the real rules: the integrated-function floor HINT reverted to the oversubscription WARNING whose action tells the owner of a working card to replace it, the outcome that rule exists to prevent. The join now runs from the controller, which carries upstream_address, and the switch-function test reads the controller's own vendor rather than its port's occupant. One controller's port resolution moves across the five captures and no finding changes there, because that platform publishes no bridge link data, so the inversion is pinned by a hand-built case. The suspicion this began as, that a card could be offered the seat it already sits in, is REFUTED: it reaches no message because every consumer compares strictly and the gain equals the current achievable bandwidth exactly - luck on an unguarded invariant, now guarded
- [x] (2026-09-12) [35] FOUND: the fixture serial guard inspected a Windows capture not at all, and its anti-vacuity test could not see that | size: one enumeration, one record reader, a per-fixture floor and a control | closed: 2026-09-12 in 66f54c3 - it enumerated drives from the Linux block and nvme sections, so a Windows capture yielded no locations and the agreement check passed vacuously, while the test beside it summed locations across every fixture against a floor the four Linux captures meet alone. An ANY test guarding an EVERY invariant. windows-ahci contributed 0 of 196 locations and now contributes 2; the floor is asserted per fixture, and a Windows control plants a serial and is proven to fail under the old enumeration. Whether that fixture's published serial is clean was not established by this guard before now
- [x] (2026-09-12) [40] FOUND: the topology tree said nothing whatever about a link it could not read | size: one branch and a test | closed: 2026-09-12 in bab7c59 - _controller_line printed the PCIe part only when the negotiated speed and width were both known, so a controller whose capture carries no link keys rendered as address, name, driver and stopped. Beside a sibling reading PCIe 3.0 x8 that blank reads as nothing to report rather than nothing readable, which is the blank-implies-fine the tool's own law forbids. Six controller lines across the five captures were silent and now state it, every one a chipset AHCI controller, which is the device the PCIe-floor rules reason about
